[package]
name = "todo-app"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.4", features = ["derive"] }
chrono = "0.4"
colored = "2.0"

[[bin]]
name = "todo"
path = "src/main.rs"
